enum TextSizes { small, medium, large }

enum OrderStatus { procession, shipped, delivered }

enum PaymentMethods {
  payPal,
  googlePay,
  applePay,
  visa,
  masterCard,
  creditCard,
  payStack,
  razorPay,
  paytm,
}
